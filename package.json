{
  "name": "ai-office-subscription-edition",
  "version": "0.4.0",
  "description": "AI OFFICE Subscription Edition with project manager, task controls, subscription automation and GitHub update workflow",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js"
  },
  "devDependencies": {
    "electron": "^37.0.0"
  }
}
